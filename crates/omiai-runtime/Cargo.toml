[package]
name = "omiai-runtime"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Minimal load-and-infer runtime for exported bundles. Must stay free of training/evolution code; compiles as lib, cdylib (FFI), and WASM targets in later slices."

[dependencies]
serde.workspace = true
serde_json.workspace = true
