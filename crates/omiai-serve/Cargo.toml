[package]
name = "omiai-serve"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "HTTP inference server: loads a model.omiai bundle, serves POST /infer (JSON in, JSON out) via axum. Scaffold — implemented in a later slice."

[dependencies]
serde.workspace = true
serde_json.workspace = true
