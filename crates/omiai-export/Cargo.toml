[package]
name = "omiai-export"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Inference bundle packaging: single model.omiai archive (tar+zstd) with versioned manifest. Scaffold — implemented in a later slice."

[dependencies]
serde.workspace = true
serde_json.workspace = true
