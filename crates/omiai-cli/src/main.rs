//! OmiAI command-line entry point: train / resume / export / bench / demo
//! subcommands over the workspace crates.

fn main() {
    // Scaffold — real clap subcommands land in later slices.
    println!("omiai CLI scaffold — subcommands arrive in later slices");
}
