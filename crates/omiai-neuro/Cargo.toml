[package]
name = "omiai-neuro"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Reservoir computing: echo-state networks with RLS readout, liquid state machines, weight utilities. CPU-only, zero-training-friendly."

[dependencies]
omiai-core = { path = "../omiai-core" }
rand.workspace = true
rand_chacha.workspace = true
rayon.workspace = true

[dev-dependencies]
criterion.workspace = true
proptest.workspace = true

[[bench]]
name = "reservoir"
harness = false
