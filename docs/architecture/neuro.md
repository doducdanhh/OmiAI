# Pillar: reservoir computing (`omiai-neuro`)

Status: **tested** — seeded sparse random reservoirs, RLS readout
training, spectral-radius normalization via power iteration (final-norm
estimator; Rayleigh quotient does not converge for complex dominant
eigenpairs). No backpropagation anywhere — CPU-only constraint.

`liquid_state` is scaffolded.
