# Pillar: evolution (`omiai-evolution`)

Status: **tested** — CGP genetic programming with island-model scaffolding
around it: selection, crossover, mutation, fitness (MSE-based) all have
real implementations exercised by integration tests (symbolic regression
on x²).
